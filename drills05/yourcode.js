function populateRecords(array){
  var newObject = {
    'charge': null,
    'cash advance': null
  }

  // for(var index = 0; index < array.length; index++) {
  //   console.log(array[index].type)

  //   if(array[index].type == 'charge'){
  //     newObject['charge'] += array[index].amount;
  //   } else if (array[index].type == 'cash advance'){
  //     newObject['cash advance'] += array[index].amount
  //   }
  // }
  // return newObject;
}
